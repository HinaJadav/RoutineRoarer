"use strict"; // use to treat all JS code as newer version

// alert("ERROR") --> this syntex is not allowed here

// Datatypes

// number => 2^53
// bigint => very large value than "number"
// string => ""
// boolean => true, false
// null => Stand Alone value
// undefined => means value still not initialized
// symbols => for defime "unique" variables

let accountType = "saving";
console.log(typeof accountType);
// "typeof" use to find datatype of variables

//** */ 
console.log(typeof null); // op: object
console.log(typeof undefined); // op: undefined
